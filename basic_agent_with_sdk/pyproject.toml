[project]
name = "basic-agent"
version = "0.1.0"
description = "Basic Ai agent using agent sdk, model gemini"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.82.0",
    "openai-agents>=0.0.16",
]
